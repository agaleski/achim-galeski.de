'use strict';
//$(function(){

/*var doc = $(document);
 var menu = $('#top');
 var menulist = $('#top-links');

 menulist.addClass('fold');

 doc.on('scroll',function(){
 var fromTop = doc.scrollTop();
 if(51 <= fromTop) {
 menu.addClass('expanded');
 }
 else if(51 > fromTop) {
 menu.removeClass('expanded');
 }
 }).scroll();*/

/*$('a#top-control').click(function(){
 menulist.toggleClass('fold');
 });*/

/*$('ul#top-links a').click(function() {
 if (location.pathname.replace(/^\//,'') === this.pathname.replace(/^\//,'') && location.hostname === this.hostname) {
 var target = $(this.hash);
 target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
 if (target.length) {
 var scrollDist,docTop,tarTop;
 docTop = doc.scrollTop();
 tarTop = target.offset().top;
 scrollDist = docTop - tarTop;
 if(docTop < tarTop) { scrollDist = tarTop - docTop; }
 $('html, body').stop().animate({ scrollTop: tarTop-50 }, scrollDist/2 +200);
 }
 }
 menulist.addClass('fold');
 return false;
 });*/

//});
